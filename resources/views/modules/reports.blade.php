<x-layouts.app title="Reports">
<div class="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm text-slate-500">Operational reports with date-range filtering and financial totals.</p><h2 class="mt-1 text-2xl font-bold text-slate-900">Business reports</h2></div><form class="flex flex-wrap items-end gap-2" method="GET"><div><label class="field-label">From</label><input type="date" name="from" value="{{ $from }}" class="px-3 py-2"></div><div><label class="field-label">To</label><input type="date" name="to" value="{{ $to }}" class="px-3 py-2"></div><button class="btn-primary">Apply filters</button></form></div>
<div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-5">@foreach([['Revenue',$summary['revenue'],'bg-indigo-50 text-indigo-600'],['Transactions',$summary['transactions'],'bg-sky-50 text-sky-600'],['Discounts',$summary['discounts'],'bg-amber-50 text-amber-600'],['Tax',$summary['tax'],'bg-emerald-50 text-emerald-600'],['Stock valuation',$summary['stock_value'],'bg-violet-50 text-violet-600']] as $metric)<div class="card p-4"><p class="text-sm font-medium text-slate-500">{{ $metric[0] }}</p><p class="mt-2 text-xl font-bold text-slate-900">{{ $metric[0] === 'Transactions' ? number_format($metric[1]) : 'Rp'.number_format($metric[1],0,',','.') }}</p><span class="mt-3 inline-block h-1.5 w-10 rounded-full {{ explode(' ', $metric[2])[0] }}"></span></div>@endforeach</div>
<div class="mt-6 grid gap-6 xl:grid-cols-2"><section class="card"><div class="card-header"><div><h3 class="font-bold text-slate-900">Sales by product</h3><p class="text-sm text-slate-500">Top ten products in the selected period</p></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Product</th><th class="text-right">Quantity</th><th class="text-right">Sales</th></tr></thead><tbody>@forelse($byProduct as $row)<tr><td class="font-semibold">{{ $row->product_name }}</td><td class="text-right">{{ number_format($row->quantity,0,',','.') }}</td><td class="text-right font-bold">Rp{{ number_format($row->total,0,',','.') }}</td></tr>@empty<tr><td colspan="3" class="py-10 text-center text-slate-400">No sales data in the chosen period.</td></tr>@endforelse</tbody></table></div></section>
<section class="card"><div class="card-header"><div><h3 class="font-bold text-slate-900">Sales by payment</h3><p class="text-sm text-slate-500">Payment-method reconciliation</p></div></div><div class="divide-y divide-slate-100">@forelse($byPayment as $row)<div class="flex items-center justify-between px-5 py-4"><div class="flex items-center gap-3"><span class="flex h-9 w-9 items-center justify-center rounded-lg bg-indigo-50 text-indigo-600">{{ strtoupper(substr($row->method,0,1)) }}</span><p class="font-semibold text-slate-800">{{ strtoupper(str_replace('_',' ',$row->method)) }}</p></div><p class="font-bold text-slate-900">Rp{{ number_format($row->total,0,',','.') }}</p></div>@empty<div class="p-10 text-center text-sm text-slate-400">No payment records in the chosen period.</div>@endforelse</div></section></div>
<section class="card mt-6"><div class="card-header"><div><h3 class="font-bold text-slate-900">Report catalogue</h3><p class="text-sm text-slate-500">The data model supports the following reporting dimensions.</p></div></div><div class="grid gap-4 p-5 md:grid-cols-2 xl:grid-cols-4">@foreach(['Sales: daily, cashier, store, product, category, payment, hour','Inventory: stock, valuation, movement, adjustment, low stock','Purchasing: purchase orders, supplier, receiving, returns','Financial: revenue, discounts, tax, refunds, cash movement, gross profit'] as $item)<div class="rounded-lg border border-slate-100 bg-slate-50 p-4 text-sm font-medium leading-6 text-slate-700">{{ $item }}</div>@endforeach</div></section>
</x-layouts.app>
